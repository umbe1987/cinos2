# cinoS

A wannabe Sonic-like platformer for the SEGA Master System written in C with SDCC and [sverx's devkitSMS](https://github.com/sverx/devkitSMS).

## How to build

Run [`build.sh`](build.sh) in your (Linux) terminal.
